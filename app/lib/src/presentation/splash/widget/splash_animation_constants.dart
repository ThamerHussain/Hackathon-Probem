class SplashAnimationDurations {
  static const logoFadeScale = Duration(milliseconds: 400);
  static const logoColor = Duration(milliseconds: 200);
  static const gradient = Duration(milliseconds: 400);
  static const text = Duration(milliseconds: 300);
}

class SplashAnimationDelays {
  static const beforeColorChange = Duration(milliseconds: 100);
  static const afterColorChange = Duration(milliseconds: 100);
  static const beforeText = Duration(milliseconds: 100);
  static const beforeComplete = Duration(milliseconds: 500);
}
